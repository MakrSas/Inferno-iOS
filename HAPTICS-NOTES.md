# Вибрация гостя на телефоне

Когда гость вибрирует, вибрирует и телефон. Сделано 14 сентября 2026: правка в
форке — `5ffd551da3 haptics: hand the guest's vibration to the embedder`,
в приложении — `app/Sources/Haptics.swift`.

Первый план — твик в UIKit гостя, который печатает маркер в консоль, как у
vphone, — отменён. Он исходил из того, что тактильный стек в виртуалке не
грузится. Это верно, пока в дереве устройств нет тактильной ветки; с ней стек
работает как на железе, и перехватывать ничего не нужно.

## Путь сигнала

    Core Haptics / UIKit в госте → HAL Actuator → i2s-порт alc2
      → SIO DMA канал 0x90 (32 бита, моно, 48 кГц)
      → hw/audio/haptics.c: кадры по 10 мс — интенсивность и частота
      → inferno_haptics_read → HostHaptics → CHHapticEngine

Приложение играет одно непрерывное событие, пока гость держит актуатор, и
двигает его интенсивность и резкость за кадрами.

## Условия

- **Нужен звук гостя** (`INFERNO_AUDIO=1`, в настройках — «Звук гостя»):
  актуатор — часть звукового железа, без него узлов в дереве нет.
- На Маке и на iPad без тактильного движка ничего не запускается: Core Haptics
  сам говорит, что играть не на чем.
- Библиотека без символа `inferno_haptics_read` — строка в логе, не ошибка.

## Как записан отсчёт

Не PCM. Отсчёт = ½·знак + член, логарифмический по модулю: 0 на полной шкале,
+0,0078 на каждое удвоение. Поэтому на проводе всегда меандр ±0,5, а сила — в
сдвиге обеих полок вниз. «Броски» до ±0,70 — самые тихие отсчёты затухания, а
не самые громкие: мерить пик |v| бессмысленно.

Резкость → частота: 0 → 80 Гц, 0,5 → 135, 1 → 230 (f = 80·2,875^s). Выше
~165 Гц синтезатор гостя сам ослабляет сигнал, эмулятор это возвращает.

**Эхо старых буферов.** Когда звуковой поток гостя опаздывает, часть DMA-буфера
уходит с содержимым прошлого буфера (63360 или 126720 байт назад) — отсюда
призрачные толчки. Эмулятор сверяет звучащие отсчёты с этими задержками и
выбрасывает серии совпадений.

## Проверка на стенде

`netlab/chap steps|transients|sweep|taps` (сборка `netlab/build-chap.sh`),
с `INFERNO_AUDIO=1`. Трасса кадров — `INFERNO_HAPTICS_TRACE=1`, сырой поток —
`INFERNO_HAPTICS_DUMP=<файл>`.
