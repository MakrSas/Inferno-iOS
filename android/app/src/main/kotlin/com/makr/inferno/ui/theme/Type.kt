package com.makr.inferno.ui.theme

import androidx.compose.material3.Typography

// The system default type ramp is Material 3's own (Roboto Flex on recent
// Android); there is no brand typeface to bring in, so this just names the
// default rather than overriding anything in it.
val InfernoTypography = Typography()
